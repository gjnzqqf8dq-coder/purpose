# Purpose — ChatGPT編集ガイド

このリポジトリの `data.json` がアプリ https://gjnzqqf8dq-coder.github.io/purpose/ の正データ。
ここを編集してコミットすれば、アプリに反映される（アプリは起動時と復帰時に読む）。

**アプリ側にキーボード入力欄は無い。入力は「声」か、この data.json の編集だけ。**
だから追加・整理・分解はここでやるのが本線。

## 構造（外から内への入れ子）

```
カテゴリ（cats）
 └ ゴール（goals）
    └ 中の目標＝マトリョーシカ（goals[].miles、3〜5個が目安）
       └ タスク（tasks、mileId で紐づく）
```

## スキーマ
```json
{
  "cats": [
    {"id":"c_shukatsu","name":"就活","hue":222,"u":1788425089000}
  ],
  "goals": [
    {"id":"g_xxx","title":"電通に受かる","deadline":"2027-06-30","note":"作戦メモ",
     "cat":"c_shukatsu","u":1788425089000,
     "miles":[{"id":"m_xxx","title":"インターンに通る","done":false}],
     "plan":{"route":[],"cargo":[],"alt":[]}}
  ],
  "tasks": [
    {"id":"t_xxx","title":"ES下書き","date":"2026-09-01","done":false,
     "goalId":"g_xxx","mileId":"m_xxx","u":1788425089000}
  ],
  "del": {}
}
```

## ルール
- `id` はユニーク。新規は `c_` / `g_` / `m_` / `t_` プレフィックス
- `u` は必ず編集時点の epoch ms（新しい方が同期で勝つ）
- 既存エントリを消さない。削除したい時は `del` に `{"<id>": <epoch_ms>}` を追加
- `date` / `deadline` は `YYYY-MM-DD`
- `cats[].hue` は 0–360 の色相。UIのカテゴリ色と背景の色味がこれで決まる
  （就活222／制作28／からだ152／学び272／くらし338）
- ゴールには必ず `cat` を付ける。分からなければ `null`（アプリ上は「未分類」）

## フライトプラン（FLIGHT PLAN）

ゴールに向かう前に「着くまでに何が起きるか」を一周見ておく層。
**アプリからは書けない（読み取りと昇格のみ）。書くのはここ。**

```json
"plan":{
  "route":[{"id":"r_1","title":"インターンを受ける","when":"2026-10","m":false}],
  "cargo":[{"id":"c_1","title":"SPIの参考書を買う","due":"2026-11-01","t":false}],
  "alt":  [{"id":"a_1","cond":"アイデアの学校に受かる","risk":"スペイン旅行に行けない",
            "hedge":"キャンセル可の航空券にする"}]
}
```

- **route**＝必ず通る工程を順に。`when` は時期（自由記述）。`m:true` は中の目標に昇格済み
- **cargo**＝要る人・モノ・お金と期限。`due` は `YYYY-MM-DD`。`t:true` はタスク化済み
- **alt**＝分岐。`cond`（もし〜なら）／`risk`（何が起きる）／`hedge`（どちらに転んでも損しない一手）

アプリ側では ROUTE の行を「中の目標に」、CARGO の行を「タスクに」ワンタップで昇格できる。
