# Purpose — ChatGPT編集ガイド

このリポジトリの `data.json` がアプリ https://gjnzqqf8dq-coder.github.io/purpose/ の正データ。
ここを編集してコミットすれば、アプリに反映される（アプリは起動時と同期ボタンで読み込む）。

## スキーマ
```json
{
  "goals": [
    {"id":"g_xxx","title":"電通に受かる","deadline":"2027-06-30","note":"作戦メモ","u":1756600000000,
     "miles":[{"id":"m_xxx","title":"インターンに通る","done":false}]}
  ],
  "tasks": [
    {"id":"t_xxx","title":"ES下書き","date":"2026-09-01","done":false,"goalId":"g_xxx","mileId":"m_xxx","u":1756600000000}
  ],
  "del": {}
}
```

構造は3層のマトリョーシカ：**ゴール → miles（マトリョーシカ＝中間ゴール、3〜5個目安） → タスク**。
タスクは `goalId` と（あれば）`mileId` で紐づく。マイルストーンの達成は `done:true`。

## ルール
- `id` はユニークな文字列（新規は `g_` / `m_` / `t_` プレフィックス推奨）
- `u` は必ず編集時点のepoch ms（これが新しい方が同期で勝つ）
- 既存エントリを消さない。削除したい時は `del` に `{"<id>": <epoch_ms>}` を追加する
- `date` / `deadline` は `YYYY-MM-DD`。`goalId` でタスクをゴールに紐づける

## フライトプラン（FLIGHT PLAN）

ゴールを立てた直後に「着くまでに実際に何が起きるか」を一周見ておくための層。
ゴールごとに `plan` を持つ。ChatGPT からもここを書いてよい。

```json
{"id":"g_xxx","title":"電通に受かる","miles":[],
 "plan":{
   "route":[{"id":"r_1","title":"インターンを受ける","when":"2026-10","m":false}],
   "cargo":[{"id":"c_1","title":"SPIの参考書を買う","due":"2026-11-01","done":false,"t":false}],
   "alt":  [{"id":"a_1","cond":"アイデアの学校に受かる","risk":"スペイン旅行に行けない",
             "hedge":"キャンセル可の航空券にする","done":false}]
 }}
```

- **route**＝ゴールまでに必ず通る工程を順に。`when` は時期（自由記述）。`m:true` は既にマトリョーシカ化済み
- **cargo**＝その工程で要る人・モノ・お金と、頼む期限。`due` は `YYYY-MM-DD`。`t:true` はタスク化済み
- **alt**＝まだ決まっていない分岐。`cond`（もし〜なら）／`risk`（何が起きる）／`hedge`（どちらに転んでも損しない今の一手）

アプリ側では ROUTE の行を「目標に」でマトリョーシカへ、CARGO の行を「タスクに」で `due` の日付のタスクへ昇格できる。
行を長押しすると消える。**新しいゴールを作ると自動でこの画面が開く**（先に一周見るのが目的なので）。
